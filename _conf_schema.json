{
  "enable": {
    "type": "bool",
    "description": "是否启用主动动作插件",
    "default": true
  },
  "classifier_provider_id": {
    "type": "string",
    "description": "意图分类 LLM Provider ID（留空使用当前默认 Provider）",
    "default": ""
  },
  "enable_image_generation": {
    "type": "bool",
    "description": "是否启用生图动作（需已安装 astrbot_plugin_aiimg_enhanced）",
    "default": true
  },
  "image_generation_mode": {
    "type": "string",
    "description": "图文顺序：before=先图后文，after=先文后图",
    "options": ["before", "after"],
    "default": "before"
  },
  "scan_incoming": {
    "type": "bool",
    "description": "是否同时扫描用户发来的消息",
    "default": false
  },
  "action_pattern": {
    "type": "string",
    "description": "自定义动作匹配正则（留空使用内置规则）",
    "default": ""
  },
  "tool_scan_enabled": {
    "type": "bool",
    "description": "动态扫描全部已注册 LLM 工具（实验性）",
    "default": false
  }
}
